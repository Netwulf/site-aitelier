import { useEffect } from 'react';

export const useScrollOptimization = () => {
  useEffect(() => {
    // Simplified - removed the scrolling class logic that was causing animation issues
    return () => {};
  }, []);
};
