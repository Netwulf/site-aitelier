# Story 2.5.1 — Archive Page Structure

**Epic:** 5 - Archive
**Priority:** P1
**Estimate:** 2-3 hours
**Status:** Ready
**Depends on:** Epic 1

---

## User Story

As a visitor, I want to access the Archive page to see the cultural output and proof of method through visual and written artifacts.

---

## Acceptance Criteria

- [ ] New page created at `/archive`
- [ ] Hero with page title
- [ ] Tab or section navigation (Visual, Journal, Obras)
- [ ] Route added to App.tsx
- [ ] Responsive design

---

## Page Structure

```
/archive
├── Hero (minimal)
├── Visual Archive (primary)
├── Journal/Ensaios
└── Obras (authorial)
```

---

## Copy

### Opening
```
ARCHIVE

A memória viva do que construímos.

Ensaios, imagens, filmes, processos.
O que fica depois que o projeto termina.
```

---

## Implementation

```tsx
// src/pages/Archive.tsx

import { useState } from 'react';
import { motion } from 'framer-motion';
import { NavigationV2 } from '@/components/NavigationV2';
import { FooterV2 } from '@/components/FooterV2';
import { SectionContainer } from '@/components/SectionContainer';
import { VisualArchiveGrid } from '@/components/VisualArchiveGrid';
import { JournalList } from '@/components/JournalList';
import { ObrasSection } from '@/components/ObrasSection';

type ArchiveTab = 'visual' | 'journal' | 'obras';

const Archive = () => {
  const [activeTab, setActiveTab] = useState<ArchiveTab>('visual');

  return (
    <div className="min-h-screen bg-brutal-black">
      <NavigationV2 />

      <main>
        {/* Hero */}
        <section className="pt-32 pb-8 px-6">
          <div className="max-w-container mx-auto">
            <motion.h1
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              className="font-display text-hero text-brutal-white mb-6"
            >
              Archive
            </motion.h1>

            <p className="text-xl text-text-secondary max-w-xl">
              A memória viva do que construímos.
              <br />
              Ensaios, imagens, filmes, processos.
            </p>
          </div>
        </section>

        {/* Tab Navigation */}
        <section className="px-6 border-b border-text-muted/20">
          <div className="max-w-container mx-auto">
            <div className="flex gap-8">
              <TabButton
                active={activeTab === 'visual'}
                onClick={() => setActiveTab('visual')}
              >
                Visual
              </TabButton>
              <TabButton
                active={activeTab === 'journal'}
                onClick={() => setActiveTab('journal')}
              >
                Journal
              </TabButton>
              <TabButton
                active={activeTab === 'obras'}
                onClick={() => setActiveTab('obras')}
              >
                Obras
              </TabButton>
            </div>
          </div>
        </section>

        {/* Content */}
        <SectionContainer>
          {activeTab === 'visual' && <VisualArchiveGrid />}
          {activeTab === 'journal' && <JournalList />}
          {activeTab === 'obras' && <ObrasSection />}
        </SectionContainer>
      </main>

      <FooterV2 />
    </div>
  );
};

const TabButton = ({
  children,
  active,
  onClick,
}: {
  children: React.ReactNode;
  active: boolean;
  onClick: () => void;
}) => {
  return (
    <button
      onClick={onClick}
      className={cn(
        "py-4 font-mono text-sm tracking-wide transition-colors",
        "border-b-2 -mb-[2px]",
        active
          ? "text-brutal-white border-ancestral-amber"
          : "text-text-muted border-transparent hover:text-text-secondary"
      )}
    >
      {children}
    </button>
  );
};

export default Archive;
```

---

## Route Update

```tsx
// src/App.tsx

import Archive from "./pages/Archive";

// In Routes:
<Route path="/archive" element={<Archive />} />
```

---

## Files to Create

- [ ] `src/pages/Archive.tsx`

## Files to Modify

- [ ] `src/App.tsx` — Add route

---

## Testing

- [ ] Page loads at /archive
- [ ] Tab navigation works
- [ ] Content switches on tab click
- [ ] URL stays at /archive (no hash change needed)
- [ ] Responsive design
