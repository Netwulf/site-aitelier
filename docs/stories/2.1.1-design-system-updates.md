# Story 2.1.1 — Design System Updates

**Epic:** 1 - Foundation & Home
**Priority:** P0
**Estimate:** 3-4 hours
**Status:** Ready

---

## User Story

As a developer, I need to update the design system to support the new Brutalist + Futuro Ancestral aesthetic so that all new components follow the unified visual language.

---

## Acceptance Criteria

- [ ] Color palette updated in CSS variables
- [ ] Typography scale updated (hero, h1, h2, body, small)
- [ ] New font imports added (Space Grotesk, JetBrains Mono)
- [ ] Spacing/grid variables updated
- [ ] Tailwind config extended with new tokens
- [ ] Global styles support Brutalist patterns

---

## Technical Details

### 1. Update CSS Variables (`src/index.css`)

```css
:root {
  /* Colors - Brutalist + Ancestral */
  --brutal-black: #0a0a0a;
  --brutal-white: #f5f5f0;
  --stone-dark: #1a1a1a;
  --stone-mid: #2a2a2a;
  --stone-light: #e5e5e0;
  --ancestral-terracotta: #c45a3b;
  --ancestral-amber: #d4a853;
  --text-primary: #f5f5f0;
  --text-secondary: #a0a0a0;
  --text-muted: #666666;

  /* Typography Scale */
  --text-hero: clamp(3rem, 8vw, 8rem);
  --text-h1: clamp(2rem, 5vw, 4rem);
  --text-h2: clamp(1.5rem, 3vw, 2.5rem);
  --text-h3: clamp(1.25rem, 2vw, 1.75rem);
  --text-body: 1rem;
  --text-small: 0.875rem;
  --text-tiny: 0.75rem;

  /* Spacing */
  --section-gap: clamp(4rem, 10vh, 8rem);
  --element-gap: clamp(1rem, 3vh, 2rem);
  --container-max: 1400px;
  --gutter: clamp(1rem, 3vw, 2rem);

  /* Transitions */
  --transition-fast: 0.2s ease;
  --transition-normal: 0.4s ease;
  --transition-slow: 0.6s cubic-bezier(0.25, 0.1, 0.25, 1);
}
```

### 2. Font Imports

Add to `index.html` or `index.css`:

```css
@import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;700&family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500&display=swap');
```

### 3. Tailwind Config Updates (`tailwind.config.ts`)

```typescript
extend: {
  colors: {
    'brutal-black': '#0a0a0a',
    'brutal-white': '#f5f5f0',
    'stone-dark': '#1a1a1a',
    'stone-mid': '#2a2a2a',
    'stone-light': '#e5e5e0',
    'ancestral-terracotta': '#c45a3b',
    'ancestral-amber': '#d4a853',
  },
  fontFamily: {
    'display': ['Space Grotesk', 'sans-serif'],
    'body': ['Inter', 'sans-serif'],
    'mono': ['JetBrains Mono', 'monospace'],
  },
  fontSize: {
    'hero': 'clamp(3rem, 8vw, 8rem)',
    'display-1': 'clamp(2rem, 5vw, 4rem)',
    'display-2': 'clamp(1.5rem, 3vw, 2.5rem)',
  },
  spacing: {
    'section': 'clamp(4rem, 10vh, 8rem)',
    'element': 'clamp(1rem, 3vh, 2rem)',
  },
  maxWidth: {
    'container': '1400px',
  }
}
```

### 4. Global Brutalist Utilities

Add to `index.css`:

```css
/* Brutalist Utilities */
.border-brutal {
  border: 1px solid var(--text-muted);
}

.border-brutal-strong {
  border: 2px solid var(--text-primary);
}

.section-number {
  font-family: 'JetBrains Mono', monospace;
  font-size: var(--text-small);
  color: var(--text-muted);
  letter-spacing: 0.1em;
}

.text-balance {
  text-wrap: balance;
}

/* Cinematic Image Treatment */
.img-cinematic {
  filter: contrast(1.1) saturate(0.9);
}

.img-grain::after {
  content: '';
  position: absolute;
  inset: 0;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
  opacity: 0.03;
  pointer-events: none;
}
```

---

## Files to Modify

- [ ] `src/index.css`
- [ ] `tailwind.config.ts`
- [ ] `index.html` (font imports)

---

## Testing

- [ ] All existing components still render correctly
- [ ] New colors visible in dev tools
- [ ] Fonts loading (check Network tab)
- [ ] No Tailwind purge issues with new classes

---

## Notes

- Keep existing brutal-black if already defined, just extend
- Preserve backward compatibility with existing components
- New components will use new tokens, old components migrate gradually
