# Story 2.4.1 — Estudos Page Structure

**Epic:** 4 - Estudos
**Priority:** P1
**Estimate:** 3-4 hours
**Status:** Ready
**Depends on:** Epic 1

---

## User Story

As a visitor interested in learning, I want to see what formation offerings AI.TELIER has, positioned as "film school without cameras" rather than typical online courses.

---

## Acceptance Criteria

- [ ] New page created at `/estudos`
- [ ] Hero with positioning statement
- [ ] Opening section explaining the approach
- [ ] Route added to App.tsx
- [ ] Navigation updated

---

## Page Structure

```
/estudos
├── Hero (minimal)
├── Opening Statement
├── Fundamentos (Vorkurs) - next story
├── Laboratórios - next story
└── Residências (brief)
```

---

## Copy

### Opening Statement
```
ESTUDOS

A escola de cinema sem câmeras.

Não ensinamos técnicas.
Formamos linguagem.

Cinema sem equipamento.
Storytelling sem fórmula.
Arte sem alienação.

Estudamos forma, ritmo, montagem, símbolo, desejo
como estruturas de pensamento —
usando IA como ferramenta, não como centro.

Não é curso online.
É imersão.
```

---

## Implementation

```tsx
// src/pages/Estudos.tsx

import { motion } from 'framer-motion';
import { NavigationV2 } from '@/components/NavigationV2';
import { FooterV2 } from '@/components/FooterV2';
import { SectionContainer } from '@/components/SectionContainer';
import { FundamentosSection } from '@/components/FundamentosSection';
import { LaboratoriosSection } from '@/components/LaboratoriosSection';

const Estudos = () => {
  return (
    <div className="min-h-screen bg-brutal-black">
      <NavigationV2 />

      <main>
        {/* Hero */}
        <section className="pt-32 pb-8 px-6">
          <div className="max-w-container mx-auto">
            <motion.h1
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              className="font-display text-hero text-brutal-white"
            >
              Estudos
            </motion.h1>
          </div>
        </section>

        {/* Opening */}
        <SectionContainer number={1}>
          <EstudosOpening />
        </SectionContainer>

        {/* Fundamentos */}
        <SectionContainer number={2}>
          <FundamentosSection />
        </SectionContainer>

        {/* Laboratórios */}
        <SectionContainer number={3}>
          <LaboratoriosSection />
        </SectionContainer>

        {/* Residências */}
        <SectionContainer number={4}>
          <ResidenciasSection />
        </SectionContainer>

        {/* Entry */}
        <SectionContainer number={5}>
          <EstudosEntry />
        </SectionContainer>
      </main>

      <FooterV2 />
    </div>
  );
};

export default Estudos;
```

### Opening Component

```tsx
// src/components/EstudosOpening.tsx

export const EstudosOpening = () => {
  return (
    <motion.div
      initial={{ opacity: 0 }}
      whileInView={{ opacity: 1 }}
      viewport={{ once: true }}
      className="max-w-3xl"
    >
      <p className="text-2xl md:text-3xl font-display text-ancestral-amber mb-12">
        A escola de cinema sem câmeras.
      </p>

      <div className="space-y-6 text-xl leading-relaxed">
        <p className="text-text-secondary">
          Não ensinamos técnicas.
          <br />
          <span className="text-brutal-white">Formamos linguagem.</span>
        </p>

        <div className="py-8 border-y border-text-muted/20 space-y-2
                        text-brutal-white">
          <p>Cinema sem equipamento.</p>
          <p>Storytelling sem fórmula.</p>
          <p>Arte sem alienação.</p>
        </div>

        <p className="text-text-secondary">
          Estudamos forma, ritmo, montagem, símbolo, desejo
          <br />
          como estruturas de pensamento —
          <br />
          usando IA como ferramenta, não como centro.
        </p>

        <p className="text-lg text-text-muted border-l-2
                      border-ancestral-terracotta pl-4 mt-8">
          Não é curso online.
          <br />
          É imersão.
        </p>
      </div>
    </motion.div>
  );
};
```

### Residências Section (Brief)

```tsx
// src/components/ResidenciasSection.tsx

export const ResidenciasSection = () => {
  return (
    <div className="py-8">
      <h2 className="font-mono text-sm tracking-widest text-text-muted mb-8">
        RESIDÊNCIAS
      </h2>

      <div className="max-w-2xl space-y-6">
        <p className="text-xl text-brutal-white">
          Períodos intensivos no campo.
          <br />
          Para quem quer ir fundo.
        </p>

        <p className="text-text-secondary">
          Presencial ou remoto.
          <br />
          Seleção por projeto.
        </p>

        <p className="text-text-muted text-sm border-l-2
                      border-text-muted/40 pl-4">
          Uma residência não é curso.
          <br />
          É tempo dedicado a uma obra
          <br />
          com o campo como contexto.
        </p>

        <p className="text-sm text-ancestral-amber font-mono mt-8">
          [Em breve: calendário 2025]
        </p>
      </div>
    </div>
  );
};
```

### Entry Section

```tsx
// src/components/EstudosEntry.tsx

export const EstudosEntry = () => {
  return (
    <div className="py-16">
      <p className="text-xl text-brutal-white mb-8">
        Interessado em entrar nos Estudos?
      </p>

      <p className="text-text-secondary mb-8">
        Mantenha-se informado sobre próximas turmas e laboratórios.
      </p>

      <a
        href="/contato?from=estudos"
        className="inline-block px-8 py-4
                   border border-brutal-white text-brutal-white
                   hover:bg-brutal-white hover:text-brutal-black
                   transition-all duration-300
                   font-mono text-sm tracking-wide"
      >
        [Entrar na lista de espera]
      </a>
    </div>
  );
};
```

---

## Route Update

```tsx
// src/App.tsx

import Estudos from "./pages/Estudos";

// In Routes:
<Route path="/estudos" element={<Estudos />} />
```

---

## Files to Create

- [ ] `src/pages/Estudos.tsx`
- [ ] `src/components/EstudosOpening.tsx`
- [ ] `src/components/ResidenciasSection.tsx`
- [ ] `src/components/EstudosEntry.tsx`

## Files to Modify

- [ ] `src/App.tsx` — Add route

---

## Testing

- [ ] Page loads at /estudos
- [ ] Navigation works
- [ ] All sections display
- [ ] Responsive design
- [ ] CTA links correctly
